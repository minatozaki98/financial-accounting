You are remediating the OWASP ZAP v2 benchmark source in the current checkout.

Model/run identity:
- Before changing files, record the displayed model label, reasoning setting if visible, start time, current branch, and current commit SHA.
- Stop and report if the model label is not the requested model for this run.

Allowed evidence:
- Use only the OWASP ZAP evidence pasted into this task by the controller.
- You may inspect the current checkout.
- Do not inspect sibling directories, other worktrees, other branches, remotes, reflogs, stashes, previous comparison reports, hidden tests, mutation manifests, reference manifests, or another model's patch.

Goal:
- Fix evidence-supported ZAP findings while preserving all API behavior, authorization boundaries, accounting invariants, audit semantics, database consistency, DTO JSON shapes, route names, and public test contracts.
- Make only changes justified by the supplied ZAP evidence and current source inspection.

Hard constraints:
- Do not add scanner/profile changes, source exclusions, disabled tests, weakened assertions, threshold changes, or feature deletion.
- Do not change benchmark infrastructure, ZAP rules, authentication workflow, scan workload, evidence scripts, or generated evidence files.
- Do not remove authorization, validation, idempotency, concurrency checks, audit writes, accounting-period rules, journal balancing, or reconciliation integrity checks to suppress alerts.
- Do not convert real security findings into redirects, 404s, empty responses, or broken workflows.
- Do not inspect or modify files outside this checkout.

Implementation expectations:
- Prefer the smallest safe source changes that remove the root cause of each alert class.
- Add or update focused public tests when changing security headers, validation, error handling, authorization, or workflow behavior.
- Keep a short interaction record with attempted fixes, commands run, changed files, elapsed time, and any remaining findings.

Verification before stopping:
- Run `dotnet test .\API\API.sln -c Debug --nologo`.
- Run `dotnet build .\API\API.sln -c Release --nologo`.
- Run `git diff --check`.
- Commit only source/test changes needed for this ZAP remediation when the controller asks you to freeze the run.

Attempt limit:
- You get one initial remediation attempt and at most two correction attempts from raw controller feedback.
- Stop after the third attempt, even if alerts remain.
