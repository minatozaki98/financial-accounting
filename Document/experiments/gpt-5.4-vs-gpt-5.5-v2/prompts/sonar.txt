You are remediating the SonarQube v2 benchmark source in the current checkout.

Model/run identity:
- Before changing files, record the displayed model label, reasoning setting if visible, start time, current branch, and current commit SHA.
- Stop and report if the model label is not the requested model for this run.

Allowed evidence:
- Use only the SonarQube evidence pasted into this task by the controller.
- You may inspect the current checkout.
- Do not inspect sibling directories, other worktrees, other branches, remotes, reflogs, stashes, previous comparison reports, hidden tests, mutation manifests, reference manifests, or another model's patch.

Goal:
- Fix evidence-supported SonarQube findings while preserving all API behavior, authorization boundaries, accounting invariants, audit semantics, database consistency, DTO JSON shapes, route names, and public test contracts.
- Make only changes justified by the supplied SonarQube evidence and current source inspection.

Hard constraints:
- Do not add NOSONAR, SuppressMessage, scanner/profile changes, source exclusions, disabled tests, weakened assertions, threshold changes, or feature deletion.
- Do not change benchmark infrastructure, scanner configuration, hidden-test assumptions, workloads, or generated evidence files.
- Do not remove authorization, validation, idempotency, concurrency checks, audit writes, accounting-period rules, journal balancing, or reconciliation integrity checks to silence findings.
- Do not inspect or modify files outside this checkout.

Implementation expectations:
- Prefer the smallest safe source changes that remove the root cause of each finding.
- Add or update focused public tests only when behavior changes or a regression risk is introduced.
- Keep a short interaction record with attempted fixes, commands run, changed files, elapsed time, and any remaining findings.

Verification before stopping:
- Run `dotnet test .\API\API.sln -c Debug --nologo`.
- Run `dotnet build .\API\API.sln -c Release --nologo`.
- Run `git diff --check`.
- Commit only source/test changes needed for this SonarQube remediation when the controller asks you to freeze the run.

Attempt limit:
- You get one initial remediation attempt and at most two correction attempts from raw controller feedback.
- Stop after the third attempt, even if findings remain.
